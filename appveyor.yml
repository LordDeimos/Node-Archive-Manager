platform: 
  - x32
  - x64

image: Visual Studio 2017

install:
  - ps: Install-Product node 4
  - npm install -g npm@next
  - npm install
  - npm install -g nodeunit

build_script:
  - npm rebuild

test_script:
  - cmd: npm test